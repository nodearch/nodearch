export enum CommandQuestionType {
  Input = 'input',
  Number = 'number',
  Password = 'password',
  List = 'list',
  RawList = 'rawList',
  Expand = 'expand',
  Checkbox = 'checkbox',
  Confirm = 'confirm',
  Editor = 'editor'
}

export enum CommandMode {
  App = 'app',
  NoApp = 'noApp'
}

export enum CliAnnotation {
  Command = 'command'
} 