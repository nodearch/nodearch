export enum CommandDecorator {
  COMMAND = '@nodearch/command/decorators/command'
}

export enum QuestionType {
  INPUT = 'input',
  NUMBER = 'number',
  PASSWORD = 'password',
  LIST = 'list',
  RAW_LIST = 'rawList',
  EXPAND = 'expand',
  CHECKBOX = 'checkbox',
  CONFIRM = 'confirm',
  EDITOR = 'editor'
}